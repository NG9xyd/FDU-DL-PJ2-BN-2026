# LaTeX 实验报告

报告源文件为 `report.tex`，使用 `ctexart` 文档类。请使用 XeLaTeX 编译：

```powershell
xelatex report.tex
xelatex report.tex
```

提交前需要替换报告中的四处红色占位符：

- 姓名
- 学号
- GitHub 仓库链接
- 模型权重下载链接

当前机器未安装 TeX 引擎，可以将整个 `report` 目录上传到 Overleaf，或安装
TeX Live/MiKTeX 后在本地编译。
